apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
 name: api-auth          # Must match name in Ingress annotation
 namespace: project-3
 annotations:
  kubernetes.io/ingress.class: kong
config:
 key_names:
   - apikey                        # Header name for the API key
 hide_credentials: true
plugin: key-auth